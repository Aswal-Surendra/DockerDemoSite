# All dependencies are open source (MIT / Apache-2.0 licensed)

# RAG orchestration framework
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-huggingface>=0.1,<0.3
langchain-chroma>=0.1,<0.3
langchain-ollama>=0.2,<0.4
langchain-text-splitters>=0.3,<0.4

# Local embedding model runtime (no API key needed)
sentence-transformers>=3.0

# Local vector database
chromadb>=0.5

# Document loaders
pypdf>=4.0
unstructured>=0.15 ; python_version >= "3.10"

# Optional REST API for the POC
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
